<Window x:Class="StatsScreen.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:StatsScreen.Views"
        Title="Stats Screen"
        Width="800"
        Height="600"
        MinWidth="200"
        MinHeight="150"
        Background="#080A0D"
        FontFamily="Segoe UI"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        WindowStartupLocation="CenterScreen"
        PreviewKeyDown="Window_OnPreviewKeyDown">
    <Grid Background="#080A0D">
        <Viewbox Stretch="Uniform">
        <Grid Width="800" Height="600">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <local:SensorTile Grid.Row="0"
                              Grid.Column="0"
                              Label="CPU TEMP"
                              Accent="#4DC6C7"
                              DataContext="{Binding CpuTemperature}" />
            <local:SensorTile Grid.Row="0"
                              Grid.Column="1"
                              Label="GPU TEMP"
                              Accent="#5F9FEA"
                              DataContext="{Binding GpuTemperature}" />
            <local:SensorTile Grid.Row="1"
                              Grid.Column="0"
                              Label="CPU POWER"
                              Accent="#D5A85B"
                              DataContext="{Binding CpuPower}" />
            <local:SensorTile Grid.Row="1"
                              Grid.Column="1"
                              Label="GPU POWER"
                              Accent="#B98BE7"
                              DataContext="{Binding GpuPower}" />
        </Grid>
        </Viewbox>

        <StackPanel Margin="0,0,17,11"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    IsHitTestVisible="False"
                    Opacity="0.78">
            <TextBlock Text="{Binding StatusText}"
                       Foreground="#718093"
                       FontFamily="Cascadia Mono,Consolas"
                       FontSize="10"
                       HorizontalAlignment="Right" />
            <TextBlock Text="{Binding LastUpdateText}"
                       Foreground="#4E5B6A"
                       FontFamily="Segoe UI"
                       FontSize="10"
                       HorizontalAlignment="Right" />
        </StackPanel>
    </Grid>
</Window>
